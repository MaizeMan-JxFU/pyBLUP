[project]
name = "pyBLUP"
version = "1.0.0"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "scipy",
    "joblib>=1.5.2"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pyBLUP"]