[project]
name = "pyBLUP"
version = "1.0.0"
requires-python = ">=3.8"
dependencies = [
    "numpy","scipy","joblib"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pyBLUP"]